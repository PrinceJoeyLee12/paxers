import React, { Fragment } from 'react';

//components
import Events from '../events/Events';

const Landing = () => {
  return (
    <Fragment>
      <Events />
    </Fragment>
  );
};

export default Landing;
